@model ProjetoSGFornecedor.UI.WEB.ViewModels.CategoriaViewModel
@{
    ViewData["Title"] = "Categoria";
}

@using (Html.BeginForm("NovaCategoria", "Categoria", FormMethod.Post))
{
    <div class="input-group flex-nowrap">
        <span class="input-group-text" id="addon-wrapping">IdCategoria</span>
        <input class="form-control"  type="text" placeholder="Id Categoria" aria-label="IdCategoria" disabled id="IdCategoria" name="IdCategoria">
    </div>
    <br>
        <label for="Nome" class="form-label">Nome</label>
         <input asp-for="@Model.Nome" class="form-control" id="Nome" type="text"  name="Nome">
    <br>
    
        @*<label for="InsertDate" class="form-label">InsertDate</label>
         <input asp-for= "@Model.InsertDate"  class="form-control"  type = "date">
        <br>
         <label for="UpdateDate" class="form-label">UpdateDate</label>
        <input asp-for= "@Model.UpdateDate"  class="form-control"  type = "date">   *@
    <br>  
    <br>
    <div class="form-check"> 
        <input class="form-check-input" type="checkbox" value="true" id="Active" name="Active">
        <label class="form-check-label" for="flexCheckIndeterminate">
            Active
        </label>
    </div>
    <br>  
    <input class="btn btn-primary" type="submit" value="Salvar">

    <a class="btn btn-primary" href="Index" >Cancelar</a>   
    
}






























@*<table class="table table-striped table-hover">
    <tr>
        <th>
            FornecedorId
        </th>
        <th>
            Active
        </th>
        <th>
            InsertDate
        </th>
        <th>
            UpdateDate
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FornecedorId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InsertDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UpdateDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.FornecedorId }, new {@class = "label label-warning" }) |
                @Html.ActionLink("Details", "Details", new { id = item.FornecedorId }, new { @class = "label label-info" }) |
                @Html.ActionLink(" ", "Delete", new { id = item.FornecedorId }, new { @class = "btn btn-danger deleteBtn btn-xs glyphicon glyphicon-trash" })
            </td>
        </tr>
    }*@

</table>
